on:
  pull_request:
    types:
      - opened

jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: automerge
        run: gh pr merge $PR_URL --admin
        env:
          GITHUB_TOKEN: "${{ secrets.PAT }}"
          PR_URL: "${{ github.event.pull_request.url }}"

